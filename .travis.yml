language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
  - "pip install coveralls"
  - "pip install numpy"
# command to run tests
script: nosetests --with-coverage --cover-package=Phon

after_success:
  - coveralls